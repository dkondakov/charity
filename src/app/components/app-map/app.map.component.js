import template from './app.map.template.html';
import controller from './app.map.controller';
import './app.map.scss';

const appMapComponent = {
  template,
  restrict: 'E',
  controller
};

export default appMapComponent;
